import VueProgressBar from 'vue-progressbar';

const options = {
  color: '#004c90',
  failedColor: 'red',
  thickness: '10px',
  transition: {
    speed: '0.2s',
    opacity: '1s',
    termination: 300,
  },
};

export { VueProgressBar, options };
